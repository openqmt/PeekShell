# 终端

PeekShell 使用 xterm.js 渲染 SSH PTY 会话。

## 多标签

每台已连接主机对应标签页，可在标签间切换。关闭标签会断开对应会话。

## 主机概览

左侧面板（可折叠）在连接后展示：

- 系统：OS、内核、架构、运行时间
- 资源：内存、交换、磁盘
- 进程概览
- 网卡流量

指标通过 SSH 周期采集，也可作为后续 AI 上下文的一部分。

## 显示设置

可在设置中配置侧栏各区块是否展示，以及主题（深色 / 浅色）与界面语言（中 / 英）。
